#include<stdio.h>
int main()
{
    int no;
    printf("Enter the number");
    scanf("%d",no);
    if(no>0)
    {
        printf("no is negative");
    }
    if(no<0)
    {
        printf("no is negative");
    }
    if(no==0)
    {
        printf("no is zero");
        }
    }